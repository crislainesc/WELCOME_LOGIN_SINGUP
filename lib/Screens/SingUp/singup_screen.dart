import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/SingUp/components/body.dart';

class SingUpScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(
        child: Column(),
      ),
    );
  }
}
